const { Controller } = require('../utils/controller')
const { Unit } = require('../utils/models')

class UnitController extends Controller {
	constructor() {		super(Unit)	}}

module.exports = new UnitController()